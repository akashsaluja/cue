export class ReminderType {
    imgPath;
    inceptionTime; // the time when first reminder was set
    timer; // time for which reminder was made
    acknowledged; // consider it synonymous to active
    reminderTime;
}